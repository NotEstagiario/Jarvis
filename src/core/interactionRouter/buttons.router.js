// src/core/interactionRouter/buttons.router.js

module.exports = async (interaction) => {
  await interaction.reply({
    content: "✅ Jarvis Alfa 0.2 ativo. (Botões ainda não implementados)",
    ephemeral: true,
  });
};
