// src/core/interactionRouter/modals.router.js

module.exports = async (interaction) => {
  await interaction.reply({
    content: "✅ Jarvis Alfa 0.2 ativo. (Modals ainda não implementados)",
    ephemeral: true,
  });
};
